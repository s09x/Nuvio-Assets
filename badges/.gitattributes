# Preserve the exact approved source, artwork and manifest bytes on all hosts.
# CRLF is a valid preserved line ending; retain normal whitespace checks.
* -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
