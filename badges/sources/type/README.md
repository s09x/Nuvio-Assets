# Historical v1 type reference

This directory retains the original v1 type/font provenance. Its framed metallic design is superseded by the approved borderless v2 release. Current typography and approval records are in `../../data/borderless-render-plan.json` and `../approved-borderless/approval.json`. The v2 renderer does not use this font or its frame geometry.
